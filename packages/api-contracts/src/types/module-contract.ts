import { CAPModule } from '@cap/shared-types'

/**
 * IModuleContract is an alias for CAPModule to standardize the Lego-style
 * module contract across the application.
 */
export type IModuleContract = CAPModule
