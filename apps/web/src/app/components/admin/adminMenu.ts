import type { IMenu } from '../layout/types'

/**
 * Placeholder admin menu configuration
 * TODO: Implement full admin menu when Admin module is ready
 */
const adminMenu: Array<IMenu> = [
  // Empty array for now - will be populated when Admin module is created
]

export default adminMenu
