{
  "name": "cap-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/modules/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@cap/web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}